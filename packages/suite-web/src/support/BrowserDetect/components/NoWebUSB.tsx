import React from 'react';

import Body from './Body';

const NoWebUSB = () => (
    <Body title="No WebUSB support" subtitle="WebUSB is only supported on Chrome for Android." />
);

export default NoWebUSB;
